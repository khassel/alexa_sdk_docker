version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
      - SDK_Version
      - WakeWordEngine
      - KittAI_Version
      - Sensory_Version
    container_name: alexa
    image: karsten13/alexa_sdk:${BuildEnv}_${WakeWordEngine}_${SDK_Version}