version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
      - myimage
      - BuildEnv
      - SDK_Version
      - KittAI_Version
    container_name: alexa
    image: karsten13/alexa_sdk:${BuildEnv}_${SDK_Version}