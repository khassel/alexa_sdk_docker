version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
#        myimage: "resin/armv7hf-debian:stretch"
        myimage: "resin/rpi-raspbian:stretch"        
    container_name: alexa
    image: karsten13/alexa_sdk:rpi