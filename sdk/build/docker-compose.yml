version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
        myimage: "karsten13/alexa_sdk_base:rpi"        
    container_name: alexa
    image: karsten13/alexa_sdk:rpi